{% macro postForm(action, csrfToken, language) %}
  <form action="{{ action }}" method="post">
    {{ caller() }}
    {# This is where the form content will be placed #}
    <input name="_csrf" value="{{ csrfToken }}"/>
    <input type="hidden" name="language" value="{{ language }}"/>
  </form>
{% endmacro %}